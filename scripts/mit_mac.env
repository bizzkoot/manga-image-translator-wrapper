# Apple Silicon defaults for manga-image-translator (no API)

# UI language in output (label used by MIT, not NLLB code)
LANG=ENG

# OCR backend: manga_ocr (JA) or alternatives (RapidOCR, etc.)
OCR=manga_ocr

# Offline translator choice
TRANSLATOR=nllb

# Detector and sizes (tune down for RAM/speed)
DETECTOR=ctd
DETECTION_SIZE=1536
INPAINTING_SIZE=1536
INPAINTING_PRECISION=bf16

# I/O
INPUT_DIR=./samples_in
OUTPUT_DIR=./samples_out

# NLLB CTranslate2 model path (produced by get_nllb_small.sh)
NLLB_MODEL_DIR=./models/nllb-200-600M-ct2

# NLLB language codes (source -> target)
# Japanese -> English by default
SRC_LANG=jpn_Jpan
TGT_LANG=eng_Latn

# Pass-through args for the translator implementation
# Adjust names to match your MIT build if needed
TRANSLATOR_ARGS="--src_lang ${SRC_LANG} --tgt_lang ${TGT_LANG} --model_dir ${NLLB_MODEL_DIR}"

# Extra CLI flags appended at the end
EXTRA_FLAGS=

# macOS: allow MPS fallbacks (safe if unused)
PYTORCH_ENABLE_MPS_FALLBACK=1

# Optional: set a font for rendering to improve fit/legibility.
# Uncomment and adjust to your preferred font path, then rerun.
# Common macOS example fonts:
#   /System/Library/Fonts/Supplemental/Arial\ Unicode.ttf
#   /System/Library/Fonts/Helvetica.ttc
#   /Library/Fonts/Arial.ttf
FONT_PATH="/System/Library/Fonts/Supplemental/Arial Unicode.ttf"
